/*
 * Empty C++ Application
 */

#include <xparameters.h>
#include <xmem.h>
#include "platform.h"

int main()
{
	init_platform();

	//intialize core (mem)

	int status;
	XMem doMem;
	XMem_Config *doMem_cfg;
	doMem_cfg = XMem_LookupConfig(XPAR_MEM_0_DEVICE_ID);
	if(!)

	return 0;
}
